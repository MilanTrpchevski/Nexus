[build]
  command = "npm run build"
  publish = "dist"

# Ensures client-side routing (if you add React Router later)
# doesn't 404 on page refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
