[build]
  command = "npm run build"
  publish = "dist"

# Required for React Router — ensures direct visits or refreshes on
# routes like /mk or /en are served index.html instead of a 404,
# letting client-side routing take over.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
